{% extends 'base.html.twig' %}

{% block title %}New Album{% endblock %}

{% block body %}
    <h1>Create new Album</h1>
    {{ include('login/_logout_form.html.twig') }}

    {{ include('album/_form.html.twig') }}

    <a href="{{ path('app_album_index') }}">back to album list</a>
    <a href="{{ path('app_artist_index') }}">back to artist list</a>
{% endblock %}
